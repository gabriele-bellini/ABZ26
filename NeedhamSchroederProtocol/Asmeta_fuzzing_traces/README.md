# Fuzzer Runs: Simple and Extended Scenarios

This folder contains four fuzzer runs. The first run represents the simplest configuration, with 3 users and a single protocol execution. The remaining runs demonstrate the scalability of the model fuzzing, using more users, messages, nonces, and protocol initiations per run. In all cases, the fuzzer successfully identifies the known Lowe vulnerability, even in substantially more complex scenarios.
